version: '3.8'

services:
  cctv-manager:
    build: .
    container_name: cctv-manager
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/New_York
    env_file:
      - .env
